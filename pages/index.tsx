import { Button } from "@mui/material";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return <Button color="error" variant="contained">Hey there</Button>;
};

export default Home;
